# Release

CalVer via `build/version.json` (`2026.1.*`). Push to `main` runs `merge.yml` and publishes packable projects to GitHub Packages.

See [novolis-governance release docs](https://github.com/Novolis-Platform/novolis-governance/blob/main/docs/release.md).
